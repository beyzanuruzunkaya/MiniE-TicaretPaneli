@* Views/Customer/AddCreditCard.cshtml *@
@model MiniE_TicaretPaneli.Models.CreditCard

@{
    ViewData["Title"] = "Yeni Kredi Kartı Ekle";
}

<h1 class="mb-4">Yeni Kredi Kartı Ekle</h1>

<div class="card shadow-sm p-4">
    <div asp-validation-summary="All" class="text-danger mb-3"></div>
    <form asp-action="AddCreditCard" method="post">
        @Html.AntiForgeryToken()

        <div class="mb-3">
            <label asp-for="CardHolderName" class="form-label">Kart Sahibi Adı Soyadı</label>
            <input asp-for="CardHolderName" class="form-control" required autocomplete="cc-name" id="CardHolderName" />
            <span asp-validation-for="CardHolderName" class="text-danger"></span>
        </div>
        <div class="mb-3">
            <label asp-for="CardNumberLastFour" class="form-label">Kart Numarası (Son 4 hane)</label>
            <input asp-for="CardNumberLastFour" class="form-control" maxlength="4" minlength="4" pattern="[0-9]{4}" required autocomplete="cc-number" id="CardNumberLastFour" />
            <span asp-validation-for="CardNumberLastFour" class="text-danger"></span>
        </div>
        <div class="row">
            <div class="col-md-6 mb-3">
                <label asp-for="ExpiryMonth" class="form-label">Son Kullanma Ayı (MM)</label>
                <input asp-for="ExpiryMonth" class="form-control" placeholder="AA" required maxlength="2" autocomplete="cc-exp-month" id="ExpiryMonth" />
                <span asp-validation-for="ExpiryMonth" class="text-danger"></span>
            </div>
            <div class="col-md-6 mb-3">
                <label asp-for="ExpiryYear" class="form-label">Son Kullanma Yılı (YY)</label>
                <input asp-for="ExpiryYear" class="form-control" placeholder="YY" required maxlength="2" autocomplete="cc-exp-year" id="ExpiryYear" />
                <span asp-validation-for="ExpiryYear" class="text-danger"></span>
            </div>
        </div>
        <div class="mb-3">
            <label asp-for="CvvHash" class="form-label">CVV (Sadece Simülasyon)</label>
            <input asp-for="CvvHash" class="form-control" type="password" placeholder="***" required maxlength="3" minlength="3" pattern="[0-9]{3}" autocomplete="cc-csc" id="CvvHash" />
            <span asp-validation-for="CvvHash" class="text-danger"></span>
            <small class="text-muted">Gerçek uygulamada CVV asla veritabanında saklanmaz. (3 haneli)</small>
        </div>

        <hr />
        <div class="d-flex justify-content-between">
            <a asp-action="CreditCards" class="btn btn-secondary">Kartlarıma Geri Dön</a>
            <button type="submit" class="btn btn-primary"><i class="fas fa-plus-circle me-2"></i>Kartı Ekle</button>
        </div>
    </form>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}